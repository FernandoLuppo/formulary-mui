export const Environment = {
    /**
     * defines the number of rows to be loaded by default in the listings
     */
    LINE_LIMIT: 10,
    /**
     * Placeholder displayed in inputs
     */
    SEARCH_INPUT: "Search...",
    /**
     * Text displayed when no records are not found in the listings
     */
    EMPTY_LISTING: "No record found.",
    /**
     * Base URL to query the data of this application
     */
    BASE_URL: "http://localhost:3001"
}
